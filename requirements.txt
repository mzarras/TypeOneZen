pydexcom>=0.2.3
python-dotenv>=1.0.0
requests>=2.28.0
# Nightscout read client (ns_sync.py, monitor.py pump rules, tz_query.py pump/now).
# Until the PyPI release is published, install with:
#   pip3 install -e ../nightscout-client
# or: pip3 install git+https://github.com/mzarras/nightscout-client.git
nightscout-client
matplotlib>=3.8
